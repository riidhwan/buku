# Use pnpm

The project standardizes on pnpm for package management. pnpm's strict dependency resolution, fast installs, disk efficiency, and lockfile behavior fit the project's strict tooling posture better than npm's more permissive defaults.
