# Enforce boundaries with lint rules

This project starts as a single Ionic Angular app and enforces architectural boundaries with TypeScript path aliases and ESLint import rules. Separate TypeScript libraries or Angular projects are deferred until the codebase is large enough to justify the additional build and maintenance overhead.
