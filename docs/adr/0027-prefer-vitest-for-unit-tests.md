# Prefer Vitest for unit tests

The project prefers Vitest for unit tests because it is fast, CI-friendly, and well suited to the unit-heavy architecture. If the current Ionic Angular toolchain makes Vitest brittle at scaffold time, the Angular-supported default runner is acceptable temporarily while keeping domain and application tests easy to migrate.
