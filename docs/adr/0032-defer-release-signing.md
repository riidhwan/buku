# Defer release signing

The initial scaffold supports Android debug builds but does not configure release signing or commit signing material. Release signing should be added when the app identity, package name, distribution path, and environment strategy are known.
