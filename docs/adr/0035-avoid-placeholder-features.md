# Avoid placeholder features

The initial scaffold should not include fake or placeholder features unless a minimal route target is required for bootstrapping. The first real feature should establish the feature pattern rather than inheriting misleading generated examples.
