# Use Ionic Angular

This project uses Ionic with Angular as the application framework. Angular is preferred because its dependency injection, service boundaries, routing conventions, forms, and test tooling support a strict, unit-testable Android-first codebase with less custom architecture than React or Vue would require.
