# Use Prettier, ESLint, and Stylelint

The project uses Prettier for formatting, ESLint for TypeScript and Angular rules, and Stylelint for CSS/SCSS rules. CI should run all formatting, script lint, style lint, tests, and build checks so styling code is held to the same strict baseline as TypeScript.
