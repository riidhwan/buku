# Use simple package scripts

Package scripts should be minimal aliases around standard tool commands rather than a custom orchestration layer. This keeps the initial scaffold transparent, easy to debug, and easy to adjust as Ionic, Angular, Capacitor, linting, and test tooling settle.
