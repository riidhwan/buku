# Limit barrel files to public boundaries

Barrel files are not generated automatically for every folder. They are allowed only at intentional public boundaries with a small API, and they must not hide internal dependency direction between domain, application, infrastructure, and presentation code.
