# Use Ionic directly in presentation

Feature presentation code may use Ionic UI components directly. Shared UI wrappers should be introduced only for proven reusable product patterns, while domain and application code remain free of Ionic dependencies.
