# Use root single-app layout

The Ionic app lives at the repository root rather than under an `apps/mobile/` monorepo layout. This keeps commands, configuration, and Capacitor/Android paths simpler while the project has only one application.
