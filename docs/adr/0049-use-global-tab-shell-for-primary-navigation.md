# Use global tab shell for primary navigation

Buku uses a global Ionic tab shell for the primary app navigation, with `Library`, `Explore`, and `More` as stable top-level product areas. The tab shell belongs to app-wide routing rather than a feature because it coordinates navigation between features, while each tab target remains a feature-owned lazy route under its own presentation layer.
