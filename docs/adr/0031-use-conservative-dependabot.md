# Use conservative Dependabot updates

The project uses Dependabot from the start with a conservative weekly schedule and no auto-merge. Dependency updates should keep Angular, Ionic, Capacitor, Android, npm tooling, and GitHub Actions current without creating excessive maintenance noise.
