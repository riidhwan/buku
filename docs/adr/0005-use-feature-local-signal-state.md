# Use feature-local signal state

Feature state is owned locally by each feature's application layer and exposed with Angular signals by default. The app does not start with a global store; global state is reserved for true app-wide concerns such as session, settings, network status, and device capabilities.
