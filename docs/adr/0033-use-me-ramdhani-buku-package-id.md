# Use `me.ramdhani.buku` as the Android package id

The Android application package id is `me.ramdhani.buku`. Choosing this before scaffolding avoids later churn across Capacitor configuration, Android manifests, Gradle files, generated package paths, and plugin state.
