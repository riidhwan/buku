# Use GitHub Actions CI

The project uses GitHub Actions for continuous integration. Workflow files live under `.github/workflows/` and should validate both the Ionic Angular application and the Capacitor Android project.
