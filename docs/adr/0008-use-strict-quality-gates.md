# Use strict quality gates from day one

The project enables strict TypeScript, strict Angular templates, type-aware ESLint rules, import boundary enforcement, formatting, and CI failures for lint warnings from the beginning. This may require cleaning up generated Ionic code, but it keeps the architecture testable and prevents weak patterns from becoming baseline.
