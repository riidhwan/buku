# Use URL slots for Explore Browser Tabs

Explore Browser Tabs are persisted URL slots selected through a single embedded WebView, not separate live browser sessions. This gives users multiple discovery destinations and fast switching while avoiding full browser-style tab history, per-tab WebView state, and a larger native browser rewrite that does not fit Explore's content-discovery scope.
