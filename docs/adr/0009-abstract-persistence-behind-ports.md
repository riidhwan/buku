# Abstract persistence behind ports

Persistence is accessed through application-layer ports and implemented by infrastructure adapters. Capacitor Preferences are reserved for small settings and flags, while SQLite is preferred for structured local data, queryable records, offline workflows, and data that needs migrations.
