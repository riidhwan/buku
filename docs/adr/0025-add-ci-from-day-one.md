# Add CI from day one

The project should include CI from the initial scaffold. CI must install with pnpm's frozen lockfile, run linting, tests, the Angular/Ionic build, Capacitor Android sync, and Android build checks so strict quality gates and native integration do not drift.
