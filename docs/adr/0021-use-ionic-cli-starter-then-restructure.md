# Use Ionic CLI starter then restructure

The project should start from the Ionic CLI Angular starter to get Ionic and Capacitor wiring quickly, then immediately tighten and reshape the generated app into the agreed architecture. Generated defaults are not the design baseline; they are scaffolding to be corrected for strict TypeScript, linting, path aliases, feature-first structure, and isolated native access.
