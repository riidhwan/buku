# Standardize the feature template

Each feature follows a standard internal structure: `domain/`, `application/`, `infrastructure/`, and `presentation/`. Application code owns use cases, facades, and ports; infrastructure owns adapter implementations and feature providers; presentation owns routes, pages, and UI components.
