# Use Ionicons only initially

The initial app uses Ionicons as the only icon library because it is the native icon set for Ionic. Additional icon libraries should be added only when a concrete product need justifies the dependency and visual-system cost.
