# Do not use Tailwind initially

The initial styling stack uses Ionic components, Ionic theme variables, and SCSS rather than Tailwind. Tailwind can be reconsidered later if real UI workflows justify utility classes, but it is not part of the baseline scaffold.
